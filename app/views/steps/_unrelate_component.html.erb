<%= form_for @step.step_components.find_by_component_id(component),
              :html => { :method => :delete } do |f| %>
    <div><%= f.hidden_field :component_id %></div>
    <div><%= f.hidden_field :step_id %></div> 
    <div class = "actions"><%= f.submit "Disassociate" %></div>
<% end %>




<li><a href="<%= problem_path(problem.id) %>">
  <% if problem.name.blank? %>
    <%= truncate(problem.statement.reverse, :length => 80).reverse %>
  <% else %>
    <%= problem.name %>
  <% end %>
</a>
</li>

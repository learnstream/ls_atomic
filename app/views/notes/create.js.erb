$("#events-area-teacher").append("<%= escape_javascript(render(@event)) %>");
$("#events-area-teacher .event").last().find(".content").markdown();
$("#add-note").html("<%= escape_javascript(render :partial => 'form') %>");
prepareNoteNew();

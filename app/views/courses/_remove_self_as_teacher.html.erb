<%= form_for(user.enrollments.find_by_course_id(course)) do |f| %>
  <div class = "field">
    <%= f.hidden_field :role, :value => "student" %>
  </div>
 <div class="actions">
   <%= f.submit "Remove as teacher" %>
 </div>
<% end %>


